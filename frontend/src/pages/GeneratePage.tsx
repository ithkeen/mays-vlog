import { PageHeader } from '../components/AppShell/PageHeader';

/**
 * GeneratePage 骨架——T1 仅渲染 PageHeader 占位，
 * 原 SubmissionWorkspace 的集成留给后续 task。
 */
export function GeneratePage() {
  return (
    <>
      <PageHeader title="Generate" />
    </>
  );
}
